# 협업 가이드

## 브랜치 규칙

기능 또는 작업 단위로 브랜치를 생성하여 작업합니다.

### 브랜치 이름

- `feat/*` : 새로운 기능 개발
- `fix/*` : 버그 수정
- `refactor/*` : 코드 리팩토링
- `test/*` : 테스트 코드 작성 및 수정
- `docs/*` : 문서 작업
- `chore/*` : 프로젝트 설정 및 기타 작업

### 작업 방식

1. 최신 `main` 브랜치에서 새로운 브랜치를 생성합니다.
2. 생성한 브랜치에서 작업하고 커밋합니다.
3. 작업이 완료되면 원격 저장소에 Push합니다.
4. `main` 브랜치를 대상으로 PR을 생성합니다.
5. PR 확인 후 `main` 브랜치에 병합합니다.
6. 병합이 완료된 작업 브랜치는 삭제합니다.


## 커밋 메시지 규칙

커밋 메시지는 아래 형식으로 작성합니다.

`타입: 작업 내용`

### 타입

- `feat:` 새로운 기능 추가
- `fix:` 버그 수정
- `refactor:` 기능 변경 없는 코드 구조 개선
- `test:` 테스트 코드 추가 및 수정
- `docs:` 문서 추가 및 수정
- `chore:` 프로젝트 설정, 패키지 관리 등 기타 작업

### 예시

`feat: 주문 생성 API 구현`

`fix: 주문 수량 검증 오류 수정`

`refactor: 주문 처리 로직 서비스 계층으로 분리`

`test: 주문 생성 API 테스트 추가`

`docs: API 명세 수정`

`chore: 프로젝트 초기 설정`


## PR 규칙

- 하나의 PR은 하나의 기능 또는 작업을 기준으로 작성합니다.
- PR 제목은 작업 내용을 명확하게 작성합니다.
- PR 본문에 주요 작업 내용을 작성합니다.
- 필요한 테스트를 완료한 후 PR을 생성합니다.
- 병합이 완료된 브랜치는 삭제합니다.