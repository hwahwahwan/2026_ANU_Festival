# Copy to .env only during the separately authorized deployment phase (chmod 600).
# raw env_file: no quotes or variable interpolation. These values initialize an EMPTY volume only.
POSTGRES_DB=festival
POSTGRES_USER=festival
POSTGRES_PASSWORD=
